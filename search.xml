<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>广播机制</title>
    <url>/2021/08/29/%E5%B9%BF%E6%92%AD%E6%9C%BA%E5%88%B6/</url>
    <content><![CDATA[<h2 id="一、广播机制简介"><a href="#一、广播机制简介" class="headerlink" title="一、广播机制简介"></a>一、广播机制简介</h2><h3 id="1-标准广播"><a href="#1-标准广播" class="headerlink" title="1.标准广播"></a>1.标准广播</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">特点：不可截断，一对多，效率高</span><br><span class="line">优点：自由控制注册和注销，灵活性好</span><br><span class="line">缺点：必须程序启动才能接收广播，所以逻辑写在Oncreate中</span><br></pre></td></tr></table></figure>

<h3 id="2-有序广播"><a href="#2-有序广播" class="headerlink" title="2.有序广播"></a>2.有序广播</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">特点：可截断，一个接一个，有优先级</span><br><span class="line">优点：程序未启动也能接收广播</span><br><span class="line">缺点：还没发现</span><br></pre></td></tr></table></figure>

]]></content>
  </entry>
  <entry>
    <title>数据存储</title>
    <url>/2021/09/09/%E6%95%B0%E6%8D%AE%E5%AD%98%E5%82%A8/</url>
    <content><![CDATA[<h1 id="文件存储"><a href="#文件存储" class="headerlink" title="文件存储"></a>文件存储</h1><h2 id="数据储存到文件中"><a href="#数据储存到文件中" class="headerlink" title="数据储存到文件中"></a>数据储存到文件中</h2><h3 id="步骤思路"><a href="#步骤思路" class="headerlink" title="步骤思路"></a>步骤思路</h3><p>（1）通过openFileOutput()方法得到一个FileOutputStream对象<br><code>out = openFileOutput ( &quot;文件名&quot; , Context.MODE_PRIVATE);</code><br>第二个参数是操作模式，有四种，常用的两种，MODE_PRIVATE（覆盖），MODE_APPEND（追加）</p>
<p>（2）借此构造一个OutputStreamWriter对象<br><code>new OutputStreamWriter(out)</code></p>
<p>（3）再借OutputStreamWriter构造出一个BufferedWriter对象<br><code>writer = new BufferedWriter(new OutputStreamWriter(out));</code></p>
<p>（4）通过BufferedWriter将数据写入文件<br><code>writer.write(data) ;</code></p>
<h3 id="完整代码示例"><a href="#完整代码示例" class="headerlink" title="完整代码示例"></a>完整代码示例</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public void save() &#123;</span><br><span class="line">    String data = &quot;Data to save&quot; ;</span><br><span class="line">    FileOutputStream out = null;</span><br><span class="line">    BufferedWriter writer = null;</span><br><span class="line">    try &#123;</span><br><span class="line">        out = openFileOutput ( &quot;data&quot; , Context.MODE_PRIVATE);</span><br><span class="line">        writer = new BufferedWriter(new OutputStreamWriter(out) );</span><br><span class="line">        writer.write(data) ;</span><br><span class="line">    &#125;catch (IOException e) &#123;</span><br><span class="line">            e.printStackTrace( );</span><br><span class="line">    &#125;finally &#123;</span><br><span class="line">        try &#123;</span><br><span class="line">            if (writer != null) &#123;</span><br><span class="line">            writer.close( ) ;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; catch (IOException e) &#123;</span><br><span class="line">        e.printStackTrace( );</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="从文件中读取数据"><a href="#从文件中读取数据" class="headerlink" title="从文件中读取数据"></a>从文件中读取数据</h2><h3 id="步骤思路-1"><a href="#步骤思路-1" class="headerlink" title="步骤思路"></a>步骤思路</h3><p>（1）通过openFileInput获取一个FileInputStream对象<br><code>in = openFileInput ( &quot;文件名&quot; ) ;</code></p>
<p>（2）借此构建出一个InputStreamReader对象<br><code>new InputStreamReader(in)</code></p>
<p>（3）接着借此构建出一个BufferedReader对象<br><code>reader = new BufferedReader(new InputStreamReader(in));</code></p>
<p>（4）通过BufferedReader对象读取数据，存放在一个StringBuilder对象中，返回读取内容<br><code>StringBuilder content = new StringBuilder( );</code><br><code>content.append (line) ;</code></p>
<h3 id="完整代码示例-1"><a href="#完整代码示例-1" class="headerlink" title="完整代码示例"></a>完整代码示例</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public String load( ) &#123;</span><br><span class="line">    FileInputStream in = null;</span><br><span class="line">    BufferedReader reader = null;</span><br><span class="line">    StringBuilder content = new StringBuilder( );</span><br><span class="line">    try &#123;</span><br><span class="line">        in = openFileInput ( &quot;data&quot; ) ;</span><br><span class="line">        reader = new BufferedReader(new InputStreamReader(in));</span><br><span class="line">        String line = &quot;&quot;;</span><br><span class="line">        while ( ( line = reader. readLine()) != null) &#123;</span><br><span class="line">            content.append (line) ;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;catch ( IOException e) &#123;</span><br><span class="line">        e.printStackTrace( ) ;</span><br><span class="line">    &#125;finally &#123;</span><br><span class="line">        if ( reader != null) &#123;</span><br><span class="line">            try &#123;</span><br><span class="line">                reader.close();</span><br><span class="line">            &#125;catch ( IOException e) &#123;</span><br><span class="line">                e.printStackTrace( );</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    return content.toString( );</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="SharedPreference存储"><a href="#SharedPreference存储" class="headerlink" title="SharedPreference存储"></a>SharedPreference存储</h1><h2 id="数据储存到SharedPreference中"><a href="#数据储存到SharedPreference中" class="headerlink" title="数据储存到SharedPreference中"></a>数据储存到SharedPreference中</h2><h3 id="步骤思路-2"><a href="#步骤思路-2" class="headerlink" title="步骤思路"></a>步骤思路</h3><p>（1）获取SharedPreferences对象(三种方法)</p>
<pre><code>getSharedPreferences(&quot;data&quot;,MODE_PRIVATE)
第一个参数：文件名
第二个参数：操作模式，只有MODE_PRIVATE

getPreferences(MODE_PRIVATE)
第一个参数：操作模式，只有MODE_PRIVATE，自动将当前活动类名作为文件名

getDefaultSharedPreferences()
调用SharedPreferences对象的exit（）方法获取对象
</code></pre>
<p>（2）向该对象中添加数据<br>    调用apply（）方法提交储存数据<br>    <code>editor.apply( );</code></p>
<h3 id="完整代码示例-2"><a href="#完整代码示例-2" class="headerlink" title="完整代码示例"></a>完整代码示例</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class MainActivity extends AppCompatActivity &#123;</span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    protected void onCreate ( Bundle savedInstanceState) &#123;</span><br><span class="line">        super.onCreate(savedInstanceState) ;</span><br><span class="line">        setContentView(R.layout.activity_main) ;</span><br><span class="line">        Button saveData = (Button) findViewById(R.id.save_data) ;</span><br><span class="line">        saveData.setOnClickListener(new View.OnClickListener() &#123;</span><br><span class="line">            @Override</span><br><span class="line">            public void onClick(View v) &#123;</span><br><span class="line">                SharedPreferences.Editor editor = getSharedPreferences(&quot;data&quot;,</span><br><span class="line">                    MODE_PRIVATE).edit();</span><br><span class="line">                editor.putString ( &quot;name&quot; ,&quot;Tom&quot; ) ;</span><br><span class="line">                editor.putInt ( &quot;age&quot; , 28);</span><br><span class="line">                editor.putBoolean ( &quot;married&quot; , false) ;</span><br><span class="line">                editor.apply( );</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="从SharedPreference中读取数据"><a href="#从SharedPreference中读取数据" class="headerlink" title="从SharedPreference中读取数据"></a>从SharedPreference中读取数据</h2><h3 id="步骤思路-3"><a href="#步骤思路-3" class="headerlink" title="步骤思路"></a>步骤思路</h3><p>（1）先获取SharedPreferences对象<br><code>SharedPreferences pref = getSharedPreferences(&quot;data&quot;，MODE_PRIVATE);</code></p>
<p>（2）用各种数据类型的get方法读取相应的数据<br><code>string name = pref.getString ( &quot;name&quot; ,&quot;&quot;) ;</code>（<strong>字符</strong>）<br><code>int age = pref.getInt ( &quot;age&quot;, 0);</code>（<strong>数字</strong>）<br><code>boolean married = pref.getBoolean ( &quot;married&quot;, false);</code>（<strong>布尔</strong>）<br>这些get方法都接收两个参数：<br>第一个参数是键，传入存储数据时使用的键就可以得到相应的值了。<br>第二个参数是默认值，即表示当传入的键找不到对应的值时会以什么样的默认值进行返回。</p>
<h3 id="完整代码示例-3"><a href="#完整代码示例-3" class="headerlink" title="完整代码示例"></a>完整代码示例</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class MainActivity extends AppCompatActivity &#123;</span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    protected void onCreate( Bundle savedInstanceState) &#123;</span><br><span class="line">        super.onCreate( savedInstanceState) ;</span><br><span class="line">        setContentView (R.layout.activity_main) ;</span><br><span class="line">        ······</span><br><span class="line">        Button restoreData = (Button) findViewById(R.id.restore_data);</span><br><span class="line">        restoreData.setOnClickListener(new View.OnClickListener() &#123;</span><br><span class="line">            @Override</span><br><span class="line">            public void onClick(View v) &#123;</span><br><span class="line">                SharedPreferences pref = getSharedPreferences(&quot;data&quot;,MODE_PRIVATE);</span><br><span class="line">                String name = pref.getString( &quot;name&quot; ,&quot;&quot;) ;</span><br><span class="line">                int age = pref.getInt(&quot;age&quot;,0);</span><br><span class="line">                boolean married = pref.getBoolean( &quot;married&quot;, false);</span><br><span class="line">                Log.d( &quot;MainActivity&quot;, &quot;name is &quot; + name) ;</span><br><span class="line">                Log.d( &quot;MainActivity&quot;, &quot;age is &quot; +age) ;</span><br><span class="line">                Log.d (&quot;MainActivity&quot;, &quot;married is &quot; +married) ;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="数据库存储"><a href="#数据库存储" class="headerlink" title="数据库存储"></a>数据库存储</h1>]]></content>
  </entry>
</search>
